const { createService } = require('./service');
const { createController } = require('./controller');
const { createRouter } = require('./router');

module.exports = { createService, createController, createRouter };
